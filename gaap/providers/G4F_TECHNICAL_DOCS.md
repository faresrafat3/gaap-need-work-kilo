# G4F (GPT4Free) - التوثيق التقني الكامل

**آخر تحديث:** فبراير 2026  
**الإصدار:** g4f v7.1.2  
**GitHub:** [xtekky/gpt4free](https://github.com/xtekky/gpt4free) - ⭐ 65,700+

---

## 📋 الفهرس

1. [ما هو g4f؟](#1-ما-هو-g4f)
2. [البنية التقنية](#2-البنية-التقنية)
3. [النماذج المجانية المؤكدة](#3-النماذج-المجانية-المؤكدة)
4. [المزودون (Providers)](#4-المزودون-providers)
5. [القيود والحدود](#5-القيود-والحدود)
6. [Stateless vs Stateful](#6-stateless-vs-stateful)
7. [نتائج البينشمارك](#7-نتائج-البينشمارك)
8. [المقارنة: g4f vs API المدفوع](#8-المقارنة-g4f-vs-api-المدفوع)
9. [البدائل المتاحة](#9-البدائل-المتاحة)
10. [الاستخدام داخل GAAP](#10-الاستخدام-داخل-gaap)

---

## 1. ما هو g4f؟

g4f (GPT4Free) هو مكتبة Python مفتوحة المصدر تجمع واجهات الذكاء الاصطناعي المجانية المتاحة على الويب في واجهة برمجة موحدة (Unified API). بدل ما تفتح ChatGPT في المتصفح أو Gemini في المتصفح، g4f بيعمل نفس الشي برمجيًا.

### كيف يعمل؟
```
المستخدم → g4f Client → Provider (مثلاً PollinationsAI) → النموذج (مثلاً Gemini 2.5)
                                                              ↓
                                                         الإجابة ترجع
```

g4f بيحاكي طلبات المتصفح (Browser Requests) للمنصات اللي بتقدم وصول مجاني للنماذج. مش reverse engineering للـ API الرسمي — بل بيستخدم واجهات مجانية فعلاً موجودة.

### التثبيت
```bash
pip install g4f[all]
# أو الحد الأدنى:
pip install g4f
```

### مثال بسيط
```python
from g4f.client import Client

client = Client()
response = client.chat.completions.create(
    model="gemini-2.5-flash",
    messages=[{"role": "user", "content": "ما هو الذكاء الاصطناعي؟"}],
    timeout=60,
)
print(response.choices[0].message.content)
```

---

## 2. البنية التقنية

### الطبقات الأساسية

```
┌─────────────────────────────────────────────┐
│              g4f.client.Client               │  ← واجهة موحدة (OpenAI-compatible)
├─────────────────────────────────────────────┤
│            Provider Router                   │  ← يختار أفضل مزود للنموذج المطلوب
├───────────┬───────────┬──────────┬──────────┤
│Pollinations│ Airforce  │HuggingFace│  Puter  │  ← المزودون المجانيون
│  (default) │ (85+ mod) │(185+ mod)│(422 mod)│
└───────────┴───────────┴──────────┴──────────┘
```

### دورة الطلب (Request Lifecycle)

1. **Client** يستقبل الطلب بصيغة OpenAI-compatible
2. **Provider Router** يبحث عن مزود مجاني يدعم النموذج المطلوب
3. **Provider** يرسل HTTP request للخدمة المجانية
4. **Response** ترجع بنفس صيغة OpenAI API

### الـ API متوافق مع OpenAI
```python
# g4f Client يشتغل بنفس طريقة OpenAI Client
response = client.chat.completions.create(
    model="...",
    messages=[...],
    timeout=60,
)
# response.choices[0].message.content
```

---

## 3. النماذج المجانية المؤكدة

تم اختبار هذه النماذج والتأكد من عملها بدون أي API key:

| النموذج | الشركة | متوسط الاستجابة | MMLU Accuracy | ملاحظات |
|---------|--------|-----------------|---------------|---------|
| **gemini-2.5-pro** | Google | ~18s | **100%** (10q) | أدق نموذج، أبطأ |
| **gemini-2.5-flash** | Google | ~9s | **94.0%** (50q) | أفضل توازن سرعة/دقة |
| **gpt-4o-mini** | OpenAI | ~5s | لم يُختبر منفرداً | جيد كـ fallback |
| **deepseek-v3** | DeepSeek | ~7s | لم يُختبر منفرداً | قوي في الاستدلال |
| **llama-4-scout** | Meta | ~1s | لم يُختبر منفرداً | الأسرع |

### نماذج تحتاج API key (لا تعمل مجاناً)
- `gpt-4o` — يحتاج OpenAI auth
- `claude-3.5-sonnet` — يحتاج Anthropic auth  
- `grok-3` — يحتاج xAI auth
- `gemini-2.0-flash-thinking` — غير متاح

---

## 4. المزودون (Providers)

### مزودون بدون تسجيل (No Auth Required)

| المزود | عدد النماذج | ملاحظات |
|--------|------------|---------|
| **PollinationsAI** | المزود الافتراضي | الأكثر استقراراً |
| **api.airforce** | 85+ | تشكيلة واسعة |
| **HuggingFace** | 185+ | أكبر مكتبة نماذج |
| **DeepInfra** | 128+ | أداء جيد |
| **Puter** | 422 | أكبر عدد نماذج مطلقاً |
| **openrouter:free** | متعدد | فلترة النماذج المجانية |

### مزودون يحتاجون تسجيل (Auth Required)

| المزود | يحتاج | ملاحظات |
|--------|-------|---------|
| OpenaiChat | __Secure-1PSID cookie | وصول لـ ChatGPT |
| Gemini | Google cookie | وصول لـ Gemini |
| Grok | xAI cookie | وصول لـ Grok |
| DeepSeek | DeepSeek token | وصول مباشر |
| GithubCopilot | GitHub token | Copilot |
| MetaAI | Meta cookie | AI Studio |

---

## 5. القيود والحدود

### Rate Limits
- **~5 requests/minute** للمزودين المجانيين
- بيتغير حسب المزود والوقت
- الحل: Round-robin بين عدة نماذج/مزودين

### حدود أخرى
- **لا يوجد ضمان SLA** — خدمة مجانية
- **Context window** — يعتمد على النموذج الأصلي (مثلاً Gemini 2.5 = 1M tokens)
- **Streaming** — مدعوم لكن مش مضمون من كل مزود
- **Timeouts** — أحياناً بتحصل، لازم retry logic

### أخطاء شائعة
```
"Missing __Secure-1PSID cookie"  → retry تلقائي، بيتحل
"Add a api_key"                  → النموذج يحتاج auth، استخدم نموذج مجاني
"Rate limit exceeded"            → انتظر أو غيّر مزود/نموذج
```

---

## 6. Stateless vs Stateful

### حقيقة مهمة: g4f بالكامل Stateless

كل طلب (request) في g4f مستقل تماماً عن اللي قبله. مفيش "محادثة" يتم تخزينها على السيرفر.

```python
# الطلب الأول
response1 = client.chat.completions.create(
    model="gemini-2.5-flash",
    messages=[{"role": "user", "content": "اسمي أحمد"}]
)

# الطلب الثاني — لا يعرف اسمك!
response2 = client.chat.completions.create(
    model="gemini-2.5-flash",
    messages=[{"role": "user", "content": "ما هو اسمي؟"}]  # ❌ لن يعرف
)
```

### كيف نحافظ على السياق؟
بإرسال تاريخ المحادثة كاملاً في كل طلب:

```python
# الطريقة الصحيحة
messages = [
    {"role": "user", "content": "اسمي أحمد"},
    {"role": "assistant", "content": "أهلاً أحمد!"},
    {"role": "user", "content": "ما هو اسمي؟"}  # ✅ سيعرف لأن التاريخ مرسل
]
response = client.chat.completions.create(model="gemini-2.5-flash", messages=messages)
```

### هل هذا يختلف عن ChatGPT في المتصفح؟
**لا.** حتى ChatGPT في المتصفح يعمل بنفس الآلية — المتصفح يرسل تاريخ المحادثة مع كل طلب. الفارق الوحيد هو أن ChatGPT يحفظ المحادثات في قاعدة بيانات للرجوع إليها لاحقاً.

### ميزة g4f على واجهة الويب
- **سياق مختلف لكل Agent** — كل agent في GAAP له messages array خاص
- **System prompts مخصصة** — تحكم كامل في شخصية كل agent
- **طلبات متوازية** — أكثر من agent يشتغل بنفس الوقت
- **Structured output** — تحليل الإجابات برمجياً

### KV-Cache: فائدة للمزود وليس للمستخدم
KV-Cache هي تقنية تسريع داخلية يستخدمها المزود (Google, OpenAI) لتسريع الاستدلال.
المستخدم لا يستفيد منها بشكل مباشر — النتيجة النهائية (الإجابة) متطابقة سواء كان هناك cache أم لا.

---

## 7. نتائج البينشمارك

### MMLU Benchmark (معيار المعرفة العامة)

| النموذج | طريقة الوصول | العينات | الدقة | التكلفة | الوقت |
|---------|-------------|---------|-------|---------|-------|
| gemini-2.5-pro | g4f مجاني | 10 | **100%** | $0.00 | ~3 دقائق |
| gemini-2.5-flash | g4f مجاني | 50 | **94.0%** | $0.00 | ~8 دقائق |
| llama-3.3-70b | API مجاني (Cerebras+Groq) | 100 | 87.0% | $0.00 | ~2 دقائق |

### الاستنتاج
g4f يحقق دقة **أعلى** من الـ API المجاني لأنه يوصل لنماذج أقوى (Gemini 2.5 vs Llama 3.3).

---

## 8. المقارنة: g4f vs API المدفوع

| المعيار | g4f (مجاني) | API مدفوع |
|---------|------------|----------|
| **التكلفة** | $0.00 | ~$5-50/شهر |
| **النماذج المتاحة** | Gemini 2.5, GPT-4o-mini, etc. | كل النماذج |
| **Rate Limit** | ~5 RPM | 60-1000 RPM |
| **الموثوقية** | متوسطة (retry مطلوب) | عالية |
| **SLA** | لا يوجد | مضمون |
| **الدقة** | 94-100% (MMLU) | نفس النماذج = نفس الدقة |
| **Context Control** | كامل | كامل |
| **للإنتاج؟** | لا يُنصح | نعم |
| **للبحث/التطوير؟** | **مثالي** ✅ | مكلف |

---

## 9. البدائل المتاحة

### مكتبات مشابهة لـ g4f

| المشروع | النجوم | اللغة | ملاحظات |
|---------|--------|-------|---------|
| **[gpt4free](https://github.com/xtekky/gpt4free)** | 65.7K ⭐ | Python | الأكبر والأشهر |
| **[gpt4free-ts](https://github.com/xiangsx/gpt4free-ts)** | 7.5K | TypeScript | نسخة TypeScript |
| **[free-one-api](https://github.com/RockChinQ/free-one-api)** | 2K+ | Python | OpenAI-compatible proxy |
| **[gradio-chatbot](https://github.com/...)** | 400+ | Python | عبر واجهات Gradio |

### خدمات API مجانية (cool-ai-stuff)

من [zukixa/cool-ai-stuff](https://github.com/zukixa/cool-ai-stuff) — قائمة منسقة لخدمات AI API المجانية:

| الخدمة | الأعضاء | GPT-4.1 | Claude 3.5 | Gemini 2.5 | DeepSeek R1 |
|--------|---------|---------|------------|------------|-------------|
| **zukijourney** | 8,058 | ✅ | ✅ | ✅ | ✅ |
| **ElectronHub** | 5,898 | ✅ | ✅ | ✅ | ✅ |
| **VoidAI** | 2,089 | ✅ | ❌ | ✅ | ✅ |
| **AnyAI** | 3,916 | ✅ | ✅ | ✅ | ✅ |

> **ملاحظة:** هذه خدمات مجتمعية، الاستقرار والاستمرارية غير مضمونة.

---

## 10. الاستخدام داخل GAAP

### الملفات ذات الصلة

```
gaap_system_glm5/
├── providers/
│   ├── chat_based/
│   │   └── g4f_provider.py          ← Provider class متكامل مع GAAP
│   ├── G4F_TECHNICAL_DOCS.md        ← هذا الملف
│   ├── PROVIDER_ARCHIVE.md          ← أرشيف مزودي API
│   └── MULTI_PROVIDER_SUMMARY.md    ← ملخص النظام متعدد المزودين
├── benchmarks/
│   ├── g4f_bench.py                 ← بينشمارك g4f (MMLU + أخرى)
│   ├── test_g4f.py                  ← اختبار أولي للنماذج
│   └── test_g4f_extended.py         ← اختبار موسع (18 نموذج)
```

### تشغيل البينشمارك
```bash
# قائمة النماذج المتاحة
python -m gaap_system_glm5.benchmarks.g4f_bench --list-models

# MMLU بـ Gemini 2.5 Flash (50 سؤال)
python -m gaap_system_glm5.benchmarks.g4f_bench \
    --dataset mmlu --samples 50 --model gemini-2.5-flash

# MMLU-Pro الأصعب (50 سؤال)
python -m gaap_system_glm5.benchmarks.g4f_bench \
    --dataset mmlu_pro --samples 50 --model gemini-2.5-flash
```

### استخدام g4f Provider في GAAP
```python
from gaap_system_glm5.providers.chat_based.g4f_provider import create_g4f_provider

provider = create_g4f_provider(default_model="gemini-2.5-flash")
# يتكامل مع نظام GAAP متعدد الطبقات
```

---

## ⚠️ تنبيه قانوني

g4f يستخدم واجهات مجانية متاحة للعموم. لكن:
- **لا تستخدمه في الإنتاج** — لا يوجد SLA أو ضمان
- **للبحث والتطوير فقط** — مثالي لاختبار الأفكار والبينشمارك
- **احترم rate limits** — لا تسيء استخدام الخدمات المجانية
- **تابع التحديثات** — المزودون يتغيرون باستمرار
